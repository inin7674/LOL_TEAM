#!/usr/bin/env sh

echo "[post-push] Deploying Cloudflare Worker..."
npx wrangler deploy --config wrangler.jsonc
status=$?

if [ $status -ne 0 ]; then
  echo "[post-push] Worker deploy failed (exit $status)."
  exit $status
fi

echo "[post-push] Worker deploy completed."
